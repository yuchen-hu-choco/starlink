#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../d3-geo-projection/bin/geo2svg" "$@"
  ret=$?
else 
  node  "$basedir/../d3-geo-projection/bin/geo2svg" "$@"
  ret=$?
fi
exit $ret
